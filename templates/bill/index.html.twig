{% extends 'base.html.twig' %}

{% block title %}Contas{% endblock %}

{% block body %}
    <div class="container d-flex align-items-center">
        <div class="row justify-content-center">
            <div class="col text-center">
                <a href="{{ path('app_asset_new') }}" class="text-decoration-none text-reset">
                    <twig:ux:icon name="ic:baseline-receipt-long" class="icon"/>
                    <h2>Adicionar conta</h2>
                </a>
            </div>
            <div class="col text-center">
                <a href="{{ path('app_asset_list') }}" class="text-decoration-none text-reset">
                    <twig:ux:icon name="ic:baseline-format-list-numbered" class="icon"/>
                    <h2>Listar contas</h2>
                </a>
            </div>
        </div>
    </div>
{% endblock %}
